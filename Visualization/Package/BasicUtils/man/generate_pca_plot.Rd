% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_PCA.R
\name{generate_pca_plot}
\alias{generate_pca_plot}
\title{Generate PCA Plot}
\usage{
generate_pca_plot(
  data,
  sample_data = NULL,
  group_var = NULL,
  method = "euclidean",
  palette = "Dark2",
  alpha = 0.75,
  lg.position = "bottom",
  plot.centroids = FALSE,
  plot_title = "PCA Plot",
  scale = TRUE
)
}
\arguments{
\item{data}{A matrix or data frame containing the data for the PCA analysis.}

\item{sample_data}{A data frame containing sample information such as group variable.}

\item{group_var}{The name of the group variable in \code{sample_data} to be used for coloring the points on the PCA plot.}

\item{method}{The method used for calculating the distance matrix. The default is "euclidean".}

\item{palette}{The color palette to be used for coloring the points on the PCA plot. The default is "Dark2".}

\item{alpha}{The transparency level of the points on the PCA plot. The default is 0.75.}

\item{lg.position}{The position of the legend on the PCA plot. The default is "bottom".}

\item{plot.centroids}{A logical value indicating whether to plot centroids for each group on the PCA plot. The default is FALSE.}

\item{plot_title}{The title of the PCA plot. The default is "PCA Plot".}

\item{scale}{A logical value indicating whether to scale the data before performing PCA. The default is TRUE.}
}
\value{
The PCA plot as an output.
}
\description{
The \code{generate_pca_plot} function is used to generate a PCA (Principal Component Analysis) plot. It takes several parameters to customize the plot, such as the data, sample information, group variable, color palette, transparency level, legend position, and more.
}
\details{
The function performs the following steps:
\enumerate{
\item Loads the necessary packages, including \code{tidyverse}, which provides a collection of packages for data manipulation and visualization.
\item Performs PCA on the input data using the specified method and scaling.
\item Extracts the principal component scores and stores them in a data frame.
\item Calculates the variance explained by each principal component.
\item If a group variable is provided, adds it to the data frame for coloring the points on the PCA plot.
\item Creates a PCA plot using \code{ggplot}, a popular plotting package in R.
\item Adds visual elements to the plot, such as points, labels, titles, color palettes, and legends.
\item If \code{plot.centroids} is set to \code{TRUE}, calculates centroids for each group and adds them to the PCA plot.
\item Returns the PCA plot as the output.
}

Example usage:

\if{html}{\out{<div class="sourceCode R">}}\preformatted{# Load data
data <- read.csv("data.csv")
sample_data <- read.csv("sample_info.csv")

# Generate PCA plot
generate_pca_plot(data, sample_data = sample_data, group_var = "group", method = "euclidean", palette = "Dark2", alpha = 0.75, lg.position = "bottom", plot.centroids = FALSE, plot_title = "PCA Plot", scale = TRUE)
}\if{html}{\out{</div>}}
}
\examples{
# Load data
data <- read.csv("data.csv")
sample_data <- read.csv("sample_info.csv")

# Generate PCA plot
generate_pca_plot(data, sample_data = sample_data, group_var = "group", method = "euclidean", palette = "Dark2", alpha = 0.75, lg.position = "bottom", plot.centroids = FALSE, plot_title = "PCA Plot", scale = TRUE)

}
